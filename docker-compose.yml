version: '3.9'


services:
  backend:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
      
    depends_on:
      - db
  
  db:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
